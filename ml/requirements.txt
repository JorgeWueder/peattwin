# PeatTwin - Modulo ML
# Entorno virtual propio:  python -m venv ml/.venv  &&  pip install -r ml/requirements.txt

numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.17.1
statsmodels==0.15.0          # STL / descomposicion estacional, pruebas estadisticas
scikit-posthocs==0.16.1      # post-hoc de Nemenyi + diagrama de diferencias criticas
joblib==1.6.0
matplotlib==3.11.1
seaborn==0.13.2              # heatmaps / EDA
jupyter==1.1.1

# Entrenamiento y comparacion de modelos (ml/training/)
xgboost==3.2.0               # Gradient Boosting
tensorflow==2.21.0           # CNN-LSTM (incluye Keras 3.x)

# ETL de flujos (ml/etl/)
pyarrow==25.0.1              # parquet
SQLAlchemy==2.0.52           # load_to_db.py y registro de modelos (reflexion del esquema)
psycopg2-binary==2.9.12      # driver PostgreSQL
